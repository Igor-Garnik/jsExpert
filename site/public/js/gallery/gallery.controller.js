export default class GalleryController {   
    controller (model, view, observer, utils){
        this.model = model;
        this.view = view;
        this.observe = observer;
        this.utils = utils
    }

}