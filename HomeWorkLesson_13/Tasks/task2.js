!		

"use strict"

let expression = "Метод substring возвращает подстроку, начиная с позиции indexA до, но не включая indexB.";

function cutString(expression){
	if(expression.lengh > 15){
	return console.log(Expression.substring(0,15) + "...");
	}
}

cutString(expression);